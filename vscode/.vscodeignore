# Exclude everything outside the vscode/ directory
../**

# Exclude dev/build artifacts inside vscode/
.claude/
.git/
.github/
.gitignore
*.vsix

# Keep README and CHANGELOG but exclude all other markdown
*.md
!README.md
!CHANGELOG.md
