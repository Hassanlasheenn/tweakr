{
  "name": "tweakr",
  "private": true,
  "version": "1.0.0",
  "description": "Visual code editor for frontend developers — hover to edit DOM elements in real time",
  "workspaces": ["extension", "server", "vscode"],
  "scripts": {
    "start": "npm run start --workspace=server",
    "dev": "npm run dev --workspace=server",
    "test": "npm run test --workspace=extension",
    "lint": "npm run lint --workspace=extension && npm run lint --workspace=server",
    "format": "npm run format --workspace=extension && npm run format --workspace=server",
    "validate": "npm run validate --workspace=extension && npm run validate --workspace=server",
    "install:all": "npm install --workspaces"
  }
}
